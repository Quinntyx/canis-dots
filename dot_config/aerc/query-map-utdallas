# aerc virtual folders -> notmuch queries (utdallas account only)
inbox=tag:inbox and not tag:deleted and path:utdallas/**
unread=tag:unread and tag:inbox and path:utdallas/**
routed=tag:routed and path:utdallas/**
sent=tag:sent and path:utdallas/**
all=path:utdallas/**
