#!/bin/sh
# goimapnotify passwordCMD: output the bare access token.
# goimapnotify builds the OAUTHBEARER/XOAUTH2 SASL response itself from the
# configured username + this token, so no encoding is needed here.

oama access dal558183@utdallas.edu
