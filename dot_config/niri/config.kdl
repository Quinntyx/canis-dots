prefer-no-csd

// --- Monitor Scaling ---
// These blocks attempt to catch standard 2080 outputs to apply 150% scaling.
// If your monitors don't scale, run `niri msg outputs` in your terminal to find 
// your exact port names (e.g., "DP-4") and replace them here.
output "DP-1" { scale 1.5; }
output "DP-2" { scale 1.5; }
output "DP-3" { scale 1.5; }
output "HDMI-A-1" { scale 1.5; }
output "HDMI-A-2" { scale 1.5; }

input {
    keyboard {
        xkb {
            // Configures layout 1 as US Workman, and layout 2 as US QWERTY
            layout "us,us"
            variant "workman,"
        }
        numlock
    }

    touchpad {
        tap
        natural-scroll
    }

    mouse { }
    trackpoint { }
}

layout {
    gaps 18  

    center-focused-column "never"

    preset-column-widths {
        proportion 0.33333
        proportion 0.5
        proportion 0.66667
    }

    default-column-width { proportion 0.5; }

    focus-ring {
        width 4 
        active-color "#ffcc00"  // Highly saturated vibrant gold/yellow
        inactive-color "#50505033"
    }

    border {
        off
    }

    shadow {
        on
        softness 35
        spread 6
        offset x=0 y=6
        color "#00000022"
        draw-behind-window false
    }

    struts { }
}

overview {
    // Zoom-out amount when the overview is open (range 0..0.75, default 0.4)
    zoom 0.16
}

layer-rule {
    match namespace="^awww-daemon$"
    place-within-backdrop true
}


blur {
    passes 5
    offset 2
    noise 0.1
    saturation 0.8
}

screenshot-path "~/Pictures/Screenshots/Screenshot from %Y-%m-%d %H-%M-%S.png"

animations { }

spawn-at-startup "waybar"
spawn-at-startup "awww-daemon"

hotkey-overlay { }

window-rule {
    match app-id=r"^org.wezfurlong.wezterm$"
    default-column-width {}
}

window-rule {
    match app-id=r"firefox$" title="^Picture-in-Picture$"
    open-floating true
}

window-rule {
    match app-id="kitty"
    draw-border-with-background false
    background-effect {
        blur false
        xray false
    }
}

window-rule {
    match app-id="pretty"
    draw-border-with-background false
    background-effect {
        blur false
        xray false
    }
}

binds {
    Mod+Shift+Slash { show-hotkey-overlay; }

    Mod+T { spawn "kitty"; }
    Mod+Space { spawn "fuzzel"; }
    Super+Alt+L { spawn "swaylock"; }
    Mod+K { spawn "nautilus"; }

    // Toggle between Workman and QWERTY
    Mod+Shift+Space { switch-layout "next"; }

    XF86AudioRaiseVolume allow-when-locked=true { spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.1+ -l 1.0"; }
    XF86AudioLowerVolume allow-when-locked=true { spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.1-"; }
    XF86AudioMute allow-when-locked=true { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"; }
    XF86AudioMicMute allow-when-locked=true { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle"; }
    XF86AudioPlay allow-when-locked=true { spawn-sh "playerctl play-pause"; }
    XF86AudioStop allow-when-locked=true { spawn-sh "playerctl stop"; }
    XF86AudioPrev allow-when-locked=true { spawn-sh "playerctl previous"; }
    XF86AudioNext allow-when-locked=true { spawn-sh "playerctl next"; }

    XF86MonBrightnessUp allow-when-locked=true { spawn "brightnessctl" "--class=backlight" "set" "+10%"; }
    XF86MonBrightnessDown allow-when-locked=true { spawn "brightnessctl" "--class=backlight" "set" "10%-"; }

    F12 repeat=false { toggle-overview; }

    Mod+Q repeat=false { close-window; }

    // Window Focusing (Workman cluster + Cross Monitor)
    Mod+Y { focus-column-or-monitor-left; }
    Mod+N { focus-window-down; }
    Mod+E { focus-window-up; }
    Mod+O { focus-column-or-monitor-right; }
    
    // Arrow keys fallback (Cross Monitor)
    Mod+Left  { focus-column-or-monitor-left; }
    Mod+Down  { focus-window-down; }
    Mod+Up    { focus-window-up; }
    Mod+Right { focus-column-or-monitor-right; }

    // Window Moving (Workman cluster + Cross Monitor)
    Mod+Ctrl+Y { move-column-left-or-to-monitor-left; }
    Mod+Ctrl+N { move-window-down; }
    Mod+Ctrl+E { move-window-up; }
    Mod+Ctrl+O { move-column-right-or-to-monitor-right; }

    Mod+Ctrl+Left  { move-column-left-or-to-monitor-left; }
    Mod+Ctrl+Down  { move-window-down; }
    Mod+Ctrl+Up    { move-window-up; }
    Mod+Ctrl+Right { move-column-right-or-to-monitor-right; }

    Mod+S { focus-workspace-down; }
    Mod+D { focus-workspace-up; }

    Mod+Ctrl+S { move-column-to-workspace-down; }
    Mod+Ctrl+D { move-column-to-workspace-up; }

    Mod+Page_Down { focus-workspace-down; }
    Mod+Page_Up { focus-workspace-up; }

    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Mod+6 { focus-workspace 6; }
    Mod+7 { focus-workspace 7; }
    Mod+8 { focus-workspace 8; }
    Mod+9 { focus-workspace 9; }

    Mod+Ctrl+1 { move-column-to-workspace 1; }
    Mod+Ctrl+2 { move-column-to-workspace 2; }
    Mod+Ctrl+3 { move-column-to-workspace 3; }
    Mod+Ctrl+4 { move-column-to-workspace 4; }
    Mod+Ctrl+5 { move-column-to-workspace 5; }
    Mod+Ctrl+6 { move-column-to-workspace 6; }
    Mod+Ctrl+7 { move-column-to-workspace 7; }
    Mod+Ctrl+8 { move-column-to-workspace 8; }
    Mod+Ctrl+9 { move-column-to-workspace 9; }

    Mod+BracketLeft  { consume-or-expel-window-left; }
    Mod+BracketRight { consume-or-expel-window-right; }

    Mod+Comma  { consume-window-into-column; }
    Mod+Period { expel-window-from-column; }

    Mod+R { switch-preset-column-width; }
    Mod+Shift+R { switch-preset-column-width-back; }

    // Resizing
    Mod+Minus { set-column-width "-10%"; }
    Mod+Equal { set-column-width "+10%"; }

    // Centering
    Mod+C { center-column; }

    Mod+F { maximize-column; }
    Mod+Shift+F { fullscreen-window; }

    Mod+M { maximize-window-to-edges; }

    Mod+V { toggle-window-floating; }
    Mod+Shift+V { switch-focus-between-floating-and-tiling; }

    Mod+W { toggle-column-tabbed-display; }

    // Screenshots
    Print { screenshot; }
    Mod+Shift+S { screenshot; }
    Ctrl+Print { screenshot-screen; }
    Alt+Print { screenshot-window; }

    Mod+Escape allow-inhibiting=false {
        toggle-keyboard-shortcuts-inhibit;
    }

    Mod+Shift+E { quit; }
    Ctrl+Alt+Delete { quit; }

    Mod+Shift+P { power-off-monitors; }
}
